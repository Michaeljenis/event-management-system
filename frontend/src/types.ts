export type studModel = {
  name: string;
  rNo: number;
  eventName: string;
  eventLocation: string;
  eventDescription: string;
};

export type updateModel = {
  rNo: number;
  eventName: string;
};